main : function void (argc : integer, argv : array [argc] integer) = {
	x : integer; // stmt_1
	a + b; //stmt_2
	{a;b;c;"ab";} // stmt_3
	{
		{
			{
				{
					"nested string";
				}
			}
		}
	}

	if (a == b)
		a = b + 1;
	else 
		a--;

	if (a < b) {
		a = c + 1;
	} else if (a == 3) {
		a++;
	} else {
		b--;
	}

	if (a == b) {
		if (c == d)
			a + 6;
		else 5-1;
		if (a == 1)
			a^9;
	} else 10--;

	
	for(i = 0; i < 100; i++) {
		print i;
		m : function integer() = {
			n : function boolean() = {
				return;
			}
			return;
		}
	}

	if (a) for(i = 0; i < 100; i++) print i; else for(i = 100; i >=0; i--) print i;

	return x(1,2,3,"true",false);

	{{{{{(1);}}}}}

	print a + 5, 4^9;

	for(;i==0;) return i;

}
